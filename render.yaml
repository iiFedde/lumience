# Render Static Site — https://render.com/docs/static-sites
services:
  - type: web
    name: stanicdesign-clone
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
